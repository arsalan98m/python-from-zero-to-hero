"""
List Methods and Properties in Python

Python's built-in list data type provides a wide range of methods and properties to efficiently store, manipulate, and access collections of data.

Key Characteristics:
- Ordered: List elements maintain the order in which they are inserted.
- Mutable: Lists can be modified after creation.
- Indexed: Each element has a specific position (index), starting from 0.
- Heterogeneous: Lists can contain elements of different data types.
- Dynamic: Lists can grow or shrink in size.
- Allows duplicates: Multiple identical values can exist in a list.

Commonly Used Methods:
- append(item): Adds an item to the end of the list.
- clear(): Removes all items from the list.
- copy(): Returns a shallow copy of the list.
- count(value): Returns the number of times a value appears in the list.
- extend(iterable): Adds all elements of an iterable to the end of the list.
- index(value): Returns the index of the first occurrence of a value.
- insert(index, item): Inserts an item at a specified position.
- pop(index): Removes and returns the item at the given index (default: last).
- remove(value): Removes the first occurrence of a value.
- reverse(): Reverses the list in place.
- sort(): Sorts the list in ascending order by default.

Special Properties and Behaviors:
- len(list): Returns the number of elements in the list.
- list[index]: Accesses the item at the given index.
- list[index] = value: Updates the item at the given index.
- list slicing: Used to extract a portion of the list (e.g., list[1:4]).

These built-in capabilities make Python lists extremely versatile for managing dynamic and heterogeneous collections of data in everyday programming tasks.
"""
